# acct shell snippets are generated by `acct hook <shell>`
# Example install:
#   eval "$(acct hook zsh)"
#   eval "$(acct hook bash)"
#   acct hook fish | source
#   Invoke-Expression (acct hook powershell)
